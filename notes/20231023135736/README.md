# Add app styles and scripts to custom views

Sometimes we want to replicate the styles of the app. To achieve this we can import the CSS and JS files using this short-codes:

```js
${appStyles}
${appScripts}
```
